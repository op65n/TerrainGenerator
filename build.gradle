plugins {
    id "com.github.johnrengelman.shadow" version "6.0.0"
    id "java"
}

group "op65n.tech.op65nterrain"
version "1.0-SNAPSHOT"

subprojects {
    apply plugin: "java"
    apply plugin: "com.github.johnrengelman.shadow"
}